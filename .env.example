# Database Configuration
# For local development with Docker
DATABASE_URL=postgresql://postgres:password@localhost:5432/shortlink

# For production, replace with your database provider URL
# Examples:
# Supabase: postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres
# Neon: postgresql://[user]:[password]@[endpoint]/[dbname]?sslmode=require
# Railway: postgresql://postgres:[password]@[host]:[port]/railway
# PlanetScale: mysql://[username]:[password]@[host]/[database]?ssl={"rejectUnauthorized":true}

# Application Configuration
# For local development
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# For production, replace with your actual domain
# NEXT_PUBLIC_BASE_URL=https://yourdomain.com

# Next.js Configuration
NEXT_TELEMETRY_DISABLED=1

# Database credentials for Docker Compose (local development only)
POSTGRES_DB=shortlink
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_PORT=5432
